name: Build and Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: windows-latest
    permissions:
      contents: write

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Create Dummy Icon (If missing)
      run: |
        if not exist assets\icon.bmp (
            echo Creating dummy icon...
            python create_dummy_icon.py
        )
      shell: cmd

    - name: Build Installer (and Main App)
      run: |
        python create_installer.py --build-only

    - name: Create Portable Zip
      run: |
        python package_portable.py

    - name: Create Release
      uses: softprops/action-gh-release@v2
      with:
        files: |
          SmartDAG_Setup.exe
          SmartDAG_Portable.zip
        generate_release_notes: true
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
